'use strict';

var {{name}} = {
	// Add ko observables here
};

{{name}}.load = function(/* params */){
	// loading of the template will go here
	// params will contain the querystring parameters as an object map
	// Default route is :view/:id, thus the entity id is in params.id.
	// I.e.: rmx.get('cases/' + params.id, ... );
	// Simply uncomment the params parameter to get started.
};

module.exports = {{name}};
